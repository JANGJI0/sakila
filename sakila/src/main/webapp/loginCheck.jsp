<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>

<%
	// 자동 로그아웃
	/* session.setMaxInactiveInterval(300); // 자동 로그아웃 5분
	
	if (session.getAttribute("loginStaff") == null) {
	    response.sendRedirect("/sakila/loginForm.jsp");
	    return;
} */
%>